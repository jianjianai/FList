{
    "installCommand": "pnpm install",
    "buildCommand": "pnpm run build",
    "outputDirectory": ".vuepress/dist",
    "routes": [
        {
            "src": "/api/verceldown/.*",
            "dest": "/api/verceldown"
        }
    ]
}